// need variables to store average, number of input scores, score total
var inputTotal = 0;
var average = inputTotal / inputCount;
var inputCount = 0;

// need prompt to allow user to enter scores
parseInt(prompt("Enter test score\nOr enter 999 to end entries"));

// need ti use an alert to display average score

